/**
 * ? import vue and router
 */
import Vue from "vue";
import Router from "vue-router";

/**
 * Use Router
 */
Vue.use(Router);

/**
 * Import components
 */


const routes = [];

export default new Router({
    mode: "history",
    routes
});